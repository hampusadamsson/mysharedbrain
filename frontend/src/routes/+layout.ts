// SPA mode: nothing is prerendered or server-rendered; the static fallback
// page boots the client router, which renders every view from the API.
export const prerender = false;
export const ssr = false;
